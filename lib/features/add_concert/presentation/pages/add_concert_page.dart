import 'package:flutter/material.dart';

class AddConcertPage extends StatelessWidget {
  const AddConcertPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Añadir concierto')));
  }
}
